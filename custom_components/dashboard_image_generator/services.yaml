generate_dashboard:
  name: Generate Dashboard
  description: Generate a new dashboard image
  fields:
    output_path:
      name: Output Path
      description: Path where to save the generated dashboard image
      required: true
      example: "/share/dashboard.png"
      selector:
        text: